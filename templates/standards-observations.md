# DEPRECATED — see `observations.md`

This template has been superseded by `observations.md` (unified observations log, T3). The two-log model is no longer used; both `workflow-observations.md` and `standards-observations.md` are consolidated under `observations.md` with a `category:` field.

If you've landed here looking for the standards-observations log, use `observations.md` and set `category: standards-coding` / `standards-testing` / `standards-naming` on your entries (per the standard you're observing).

This file is preserved only as a redirect; it will be removed in a future workflow revision.
