# DEPRECATED — see `observations.md`

This template has been superseded by `observations.md` (unified observations log, T3). The two-log model (`workflow-observations.md` + `standards-observations.md`) is no longer used; both are consolidated under `observations.md` with a `category:` field to distinguish workflow-defect entries from standards-* entries.

If you've landed here looking for the workflow-defects log, use `observations.md` and set `category: workflow-defect` on your entries.

This file is preserved only as a redirect; it will be removed in a future workflow revision.
